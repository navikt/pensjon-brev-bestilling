apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: pensjon-brev-bestilling
  namespace: pensjonsbrev
  labels:
    team: pensjonsbrev
spec:
  image: {{image}}
  accessPolicy: 
    external:
        - host: api-gw-t4.adeo.no